> [!example]+ Пример
> ```py
> a += b 
> # a = a + b
> a -= b  
> # a = a - b
> a *= b 
> # a = a * b
> a /= b 
> # a = a / b
> a //= b 
> # a = a // b
> a %= b 
> # a = a % b

`a += 1` - Увеличение на 1