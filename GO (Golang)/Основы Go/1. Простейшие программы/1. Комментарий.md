Закомментировать одну строку кода можно с помощью `//`:

>[!code]+ Example
>```go
> // Этот код не читается
>```

Несколько строк комментируют с помощью открытия и закрытия символов `/*` и `*/` :
>[!code]+ Example
>```go
> /* Этот код не читается
> этот код не читается */
>```